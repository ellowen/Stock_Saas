# Typography

No se confirmó una fuente custom cargada (`@font-face`) en esta ronda — probablemente la stack default de sistema de Tailwind (`font-sans`). Tamaños de texto: se usan las utilidades default de Tailwind (`text-sm`, `text-base`, `text-lg`, etc.) sin una escala tipográfica documentada aparte. Pendiente de auditoría real si se decide formalizar (extraer qué tamaño/peso usa cada nivel: título de página, título de card, label, valor de dato, texto de ayuda) en vez de asumir sin haber leído cada componente.
