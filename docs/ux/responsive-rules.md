# Responsive Rules

Ver `RESPONSIVE.md` (raíz) para el contenido completo. Regla central en una línea: en `<640px`, colapsar información secundaria detrás de un toggle explícito en vez de re-flowear el mismo layout a una columna angosta — establecida en el rediseño del POS, no confirmada como aplicada parejo en el resto de los módulos.
