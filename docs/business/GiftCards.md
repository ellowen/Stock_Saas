# Business Rules — Gift Cards

**No implementado.** No hay modelo `GiftCard` ni ningún `PaymentMethod` que la represente (el enum real es `CASH/CARD/MIXED/OTHER/CREDIT`, sin un valor para tarjeta de regalo). Si se necesita, requiere modelo nuevo (código, saldo, vigencia) e integración con el flujo de cobro del POS como un método de pago adicional.
